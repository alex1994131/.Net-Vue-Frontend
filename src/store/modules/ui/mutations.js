export default {
    // communicate directly with the state variables
    setLoading : (state,loading) => state.loading = loading,
    setCounts : (state,counts) => state.counts = counts,
    setNotes : (state,notes) => state.notifications = notes

 }